// @todo API works from node, node-webkit and console.
var CLIArgs = require('CLIArgs');

// @todo require all modules

CLIArgs.process();

if(!CLIArgs.has('GUI')){
	// if don't have GUI, then it's called via console.
	
	
}


console.log( 'JSDOCGUI API CHECKS FOR GUI EXISTS: ', CLIArgs.has('GUI') );
